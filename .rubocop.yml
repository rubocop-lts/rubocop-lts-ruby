inherit_gem:
  rubocop-lts: config/rubygem_rspec.yml
inherit_from:
  - .rubocop_rspec.yml
plugins:
  - rubocop-rspec
  - rubocop-on-rbs
  - rubocop-packaging
# This tells RuboCop to add the exclusions in this file
# to the default exclusions (and those from other inherited files),
# rather than overwriting them.
inherit_mode:
  merge:
    - Exclude
AllCops:
  Exclude:
    - certs/**/*
    - checksums/**/*
    - docs/**/*
    - examples/**/*
    - fixtures/**/*
    - gemfiles/*.gemfile
    - gemfiles/vendor/**/*
    - pkg/**/*
    - README.md
    - results/**/*
    - spec/fixtures/**/*
    - tmp/**/*
    - vendor/**/*
    - "**/vendor/**/*"
    - AGENTS.md
  NewCops: enable
RBS:
  Enabled: true
Layout/IndentationConsistency:
  Exclude: ['*.md']

# Appraisal uses this filename for the moving HEAD dependency fixture.
Naming/FileName:
  Exclude:
    # This hyphenated entrypoint is the conventional require path for plugin gems.
    - lib/rubocop-lts-ruby.rb
